// Declaramos e inicializamos el string con "" o ''
const producto = "Monitor 20 pulgadas";
console.log(producto);

// Declaramos e inicializamos el string con String
const producto2 = String('Monitor 24 pulgadas');
console.log(producto2);

// Declaramos e inicializamos el string como un objeto
const producto3 = new String('Monitor 27 pulgadas');
console.log(producto3);

// Escape de comillas utilizando \
const producto4 = "Monitor x \"";
console.log(producto4);