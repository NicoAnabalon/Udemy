// Inicializar la variable
let producto = 'Tablet';
console.log(producto);

// Reasignamos la variable con un string
producto = "Monitor";
console.log(producto);

// Reasignamos la variable con un int
producto = 20;
console.log(producto);

// Reasignamos la variable con un bool
producto = true;
console.log(producto);

// Reasignamos la variable con un null
producto = null;
console.log(producto);

// Declaramos la variable sin inicializarla
let precio;
console.log(precio);

// Inicializamos la variable
precio=300;
console.log(precio);