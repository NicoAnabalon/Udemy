const producto = {
    nombre: "Monitor 20 Pulgadas",
    precio: 300,
    disponible: true
}

// Obteniendo las keys del objeto (Propiedades)
console.log("Object keys:", Object.keys(producto));

// Obteniendo los valores del objeto
console.log("Object values:", Object.values(producto));

// Obteniendo keys y values
console.log("Object entries:", Object.entries(producto));